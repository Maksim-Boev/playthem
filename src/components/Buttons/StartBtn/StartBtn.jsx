import React from 'react';
import StyledStartBtn from './StyledComponent';

const StartBtn = ({ width }) => {
  return <StyledStartBtn w={width}>Get Started Now</StyledStartBtn>;
};

export default StartBtn;
